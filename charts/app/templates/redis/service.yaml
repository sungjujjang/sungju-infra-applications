{{- if .Values.redis.enabled }}

apiVersion: v1
kind: Service

metadata:
  name: {{ include "app.name" . }}-redis

spec:
  type: ClusterIP

  selector:
    app: {{ include "app.name" . }}-redis

  ports:
    - name: redis
      port: 6379
      targetPort: 6379

{{- end }}