replicaCount: 2

image:
  repository: nginx
  tag: "latest"

service:
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  host: my-app.example.com

mysql:
  enabled: true

redis:
  enabled: true